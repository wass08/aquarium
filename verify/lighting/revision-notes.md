Lamp / floor / shadow revision

- Floor caustics now require a forward ray (toward the lamp) to overlap the live water AABB. The slab-overlap mask fades inward over 0.15 world units; waterHeight controls the upper slab. The plain blue pool is independent of that mask and fades to zero 2.5 units outside the plinth.
- Head (-3.4, 5.25, -2.4), target (0.2, 0.9, 0.2), elevation 44.4087 degrees. The head is 0.50 units closer horizontally and 0.25 lower. The spotlight half-angle is 43 degrees. Shadow focus 1.25 covers the near plinth corners; fit.mjs still tests every actual bevel vertex against the perspective frustum and now correctly measures its margin using shadow-camera FOV rather than illumination-cone angle.
- Base: 0.98 outer radius, 0.24-thick dark body, 0.065-thick brass top, collar and brass rocker. Brass matches the existing plaque's #bd975b (the plaque is actually in stage.ts). A procedural anisotropic roughness field suggests brushed metal.
- Pointer switch raycast runs before the glass test and consumes the click. L uses the same toggle. A real-time 0.35-second smoothstep ramp is independent of slow motion and rewind. Spot, diffuser, caustics, pool, shafts, mote brightening and analytic water sun glints share lampOn. Cool ambient and faint ambient motes remain off.
- Diagnosis: shadow-debug-before.png was captured before changes to lighting/shadow/coral settings. The map already contained island self-shadow and gravel shadows; corals had receiveShadow but no castShadow. The perspective divide and 4x4 comparison filter were working. Ambient fill and additive caustics masked the visible sand shadow. Water and glass were not shadow casters.
- Fix: all four reef batches cast; gravel continues casting. Shadow intensity 0.90 and normalBias 0.028; reduced sand/gravel caustic emission and shadow-aware indirect fill preserve their contact silhouettes. Island retains its original albedo/emissive treatment and the runtime contrast bounds. Glass panels and water surface explicitly do not cast or receive.
- Performance: the first runtime attempt failed the floor camera at 51 FPS. Sharing the same ShadowNode between standard lighting, procedural emission, contact fill and floor eliminated redundant shadow renders (default draw calls 156 -> 116). See runtime-attempt1.txt and the final runtime.txt rather than treating the first attempt as a pass.
- Repro: npm run build; npm run preview -- --port 4174; node verify/runtime.mjs; node verify/lighting/fit.mjs; node verify/lighting/revision.mjs. The latter uses the agent-browser skill/CLI and records real pointer-switch and keyboard ramp assertions, captures and console errors in revision.json.
- Requested evidence: floor-wide.png shows the compact downstream caustic footprint; lamp-side-floor.png shows plain near-side floor with the far-side caustics partly hidden by the aquarium; lamp-base.png shows both discs, collar and rocker; lamp-off.png shows an unlit grey diffuser, no floor pool or shafts and a dim but readable tank; shadows.png and shadows-closeup.png show the island shadow at the right peak's foot plus coral/gravel silhouettes; shadow-debug.png isolates the shadow term with transparent overlays hidden. The debug image exposes fine PCF banding on steep backfaces; normal materials make it much less apparent.
- This revision did not edit Lab, worley or the other agents' assigned files; no tank.ts hook is needed. Hashes detected concurrent external edits to src/lab/caustics.ts, src/lab/levels.ts and src/lib/worley.ts during verification. Those changes were left intact.

Final unmodified runtime suite: PASS. Every camera and shattered-state median is 60 FPS at pixel ratio 1.5. An intermediate run failed the physics 3–4-second settling assertion (runtime-attempt2.txt); the final run recorded zero displacement and passed both settling windows without changes to the test. Both upper-plinth (24 vertices) and rounded-base (900 vertices) frustum tests pass.
