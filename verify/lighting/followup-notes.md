Lamp follow-up

Audio: 12 ms bandpass noise (ON 2100 Hz / OFF 1600 Hz) plus a 75 ms falling sine thump (155 / 120 Hz). ON adds a very quiet 300 ms 62 Hz swell. All sources connect to the existing mute gain and join the reset voice set. An automatic click never resumes a suspended context; a manual click/key can finish the existing gesture resume. No sound assets or dependencies added. audio-check.json records locked-load silence, physical rocker and keyboard playback, mute/unmute and no startup replay on reset.

Startup: lampOn begins at zero. After illuminated and dark warm-up renders finish their GPU submissions, the first visible ready frame starts the timeline at zero. The rocker clicks at .18 s, pulses peak at .20 and .40 s, and an .08-to-1 smooth ramp runs from .60 to 1.20 s. User toggles cancel the sequence and use the normal .35 s ramp. R never restarts it. lampSequenceDone and lampSequenceTime join lampOn in diagnostics. runtime.mjs only changed its initial/preset lamp-ready waits; contrast, shadow-luminance and FPS thresholds are unchanged.

The six power-on PNGs are real canvas frames (without HTML UI), captured after the application's render callback at the requested 0, .2, .4, .6, .9 and 1.3 s targets. power-on.json stores actual frame times and lamp values. Capture does not freeze or alter the lamp clock. The first is ambient-only; the next two show the separate flash peaks, the fourth shows the dim trough, the fifth the final rise, and the last steady illumination. The render-callback capture is necessary because WebGPU clears its presentation buffer before an earlier-registered RAF callback can read it. GPU warm-up is finished under the loading overlay so initial readback cannot consume the first flash.

Pose: head (-4.12,4.65,-2.92), unchanged target (.2,.9,.2), elevation 35.1346109 degrees. The old horizontal head-to-target vector is multiplied by exactly 1.2, preserving its azimuth. Half-angle 50 degrees, penumbra .62, shadow focus 1.08 (54 degree shadow half-FOV). The integrated floor pool tapers to zero at 4.8 units outside the plinth; the live-water slab test still excludes upstream caustics. Near-side illumination is dimmer because of the oblique cone. The rebuilt continuous arm joins the existing base and lowered housing. The default view contains the entire lamp head.

Shadow diagnosis: causticLight and rockCaustics were already multiplied by the shared shadow term. The remaining .90 shadow intensity leaked 10% of caustic emission into umbra; the old steep ray shortened the island footprint; the island's .80 ambient-occlusion value and unoccluded .045 emissive fill flattened far flanks. The new map uses intensity 1, 3072-square resolution, normalBias .012 and bias -.00006, with the same perspective-aware 4x4 filter (2.2-texel spacing). Occluded island fill is attenuated (AO .60 vs .80; emission .0225 vs .05). Sand AO is .70 in shadow and .85 outside: this retains the requested shadow luminance while direct light and caustics are completely blocked. A cooler ambient floor survives even in the plinth shadow. Corals still cast and water/glass still do not.

Measurement: sand-measure.mjs reads linear Rec.709 luminance from 3x3 patches over 30 frames. Geometry rays identify 61 visible sand sites shadowed specifically by the island and 23 lit sites within .65 world units of those shadows. Four surrounding ray tests exclude terminator edges; camera rays reject rock, gravel and coral pixels. The viewport is fixed at 1920x1080 before loading the page, so cached projected probes match the rendering. Final measurements and all world/pixel coordinates are in sand-luminance.json and sand-measure.txt. These measurements include the real transparent water/glass and moving caustics.

Evidence: idle-default.png and floor-wide.png show the lower head, wider pool and longer downstream footprint. shadows.png is the default fixed view; shadows-closeup.png shows the darker far flanks and cast region around the foot, plus small coral/gravel shadows. The sand is visibly darker but retains texture; underwater glass/reflections still soften apparent contrast. shadow-debug.png isolates actual map visibility. Lamp shadow fit passes all 900 rounded-base and 24 upper-plinth vertices. followup-runtime.txt is the dedicated log of the full passing runtime suite at 60 FPS, isolated from concurrent agents' shared summary files.

Only owned lighting/audio/app/verification files were edited. No Lab, Worley, physics, shatter, tank, spill, rewind, post, styles or index edits were made by this revision. Other agents' concurrent source changes were left intact.
